services:
  db:
    image: postgres:16
    container_name: movierizerDatabase
    restart: always
    env_file:
      - env
    ports:
      - "5400:5400"
  

  adminer:
    image: adminer
    restart: always
    ports:
      - 8081:8080